# StaticRouter

## Components

```ts
interface StaticRouterProps {
  routes: Route;
  location: string;
  matches: RouteMatch[];
}

declare function StaticRouter({ routes, location, matches }: StaticRouterProps);
```
